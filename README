This project is a functional command shell, it supports most of commands in a normal bash shell including I/O redirection and pipeline. 

The detailed description is in commandShell.txt

Please compile the program with command: make
Please de-compile with command: make clean

Once compiled, an executable called "myshell" would be created, simply run "./myshell" and the bash shell would prompt.